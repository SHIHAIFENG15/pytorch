# 硬件解耦任务交付模板

---

## 0. 开发 & 检视标准

> 本节仅为交付标准参考，无需在交付文档中体现。

### 通用标准

1. **最小化 Diff** — 一个 PR 只做解耦，不附带无关变更（重构、重命名、格式化等）。
2. **控制 PR 规模** — 尽量只修改一个文件，单个 PR 修改行数尽量不超过 200 行。
3. **及时回复检视意见** — 对社区 reviewer 的检视意见及时响应，避免 PR 长期停滞。
4. **RFC 对齐** — 若涉及大范围接口变更，实现方向必须与 RFC 一致。

### 用例解耦

**核心原则：上游社区用例实际执行数量不能因解耦发生变化。**

1. **社区设备生成路径不变** — 新增 NPU 注册时，不能修改社区设备的 `device_type` 列表构造逻辑，只能追加，不能删除、重排或改变条件分支。
2. **社区设备守卫语义不变** — 对社区已支持设备而言，所有 skip/only 的判定结果必须与解耦前等价。
3. **可验证** — 在社区 CI 环境中运行 `pytest --collect-only`，解耦前后 collected tests 数量和名称必须完全一致。

### 特性解耦

**核心原则：尽量复用现有方案，避免重复造轮子；解耦前后社区功能行为不变。**

1. **优先复用社区已有扩展机制** — 只有现有机制确实无法满足时，才设计新机制，且 API 风格需与社区已有接口一致。
2. **功能等价性** — 社区已有设备在解耦前后走到的代码路径、产生的行为完全一致。
3. **默认行为不变** — 新增注册机制时，未注册的设备仍走原有默认路径，不报错、不跳过。

---

## 1. 背景

> 简要描述本任务要解决的问题，说明为什么需要做这件事。

**涉及模块：**（如 Dynamo / Inductor / Distributed / OpInfo / ...）

**任务类型：** 特性硬件解耦 / 用例硬件解耦

**问题描述：**

（示例）
- PyTorch 的 Inductor 设备判定写死了 `["cuda", "xpu"]`，NPU 被排除在编译优化路径之外
- 社区测试文件 `test_xxx.py` 使用 `@onlyCUDA` 守卫，NPU 无法复用

---

## 2. 方案

> 描述采用的技术方案，说明如何解决问题。

**方案概述：**

（示例）
- 特性解耦：将设备判定逻辑从硬编码列表改为注册机制，各后端通过 `register_backend(...)` 自行注册
- 用例解耦：将测试类标注为 `DEVICE_GENERIC`，通过 `instantiate_device_type_tests` 生成 NPU 变体

**涉及的文件：**

| 仓库 | 文件 | 变更说明 |
|---|---|---|
| pytorch/pytorch | `torch/_inductor/scheduler.py` | 新增 `_registered_device_guards` dict，`device_need_guard` 改为查表 |
| pytorch/pytorch | `test/inductor/test_scheduler.py` | `@skipIf(not HAS_GPU)` → `hardware_requirement = DEVICE_GENERIC` |
| Ascend/pytorch | `torch_npu/_inductor/__init__.py` | 删除 Patch，改为调用社区注册接口 |

**RFC 评审要求：**

> 若方案涉及对上游社区**大范围的接口变更或架构调整**（如新增注册机制、扩展 dispatch 路径、修改公共 API 签名等）：
>
> - **已有 RFC** → 直接贴上 RFC 链接：___
> - **需要但尚无 RFC** → 遵循以下流程：
>   1. 与对应模块的 **SE** 沟通方案
>   2. 在 **SEG/SIG** 上进行评审
>   3. 评审通过后向 PyTorch 社区提交 **RFC 提案**
>   4. 在本交付文档中补充 RFC 提案链接

---

## 3. PR

### 3.1 社区接纳标准

社区 PR 满足以下任一条件，视为**被社区接纳**：

| 状态 | 判定依据 |
|---|---|
| **已合入（Merged）** | PR 状态为 `merged`，commit 已进入 PyTorch 主分支 |
| **已批准（Approved）** | 至少一位 PyTorch Maintainer 给出了 `Approved` review，PR 等待合并 |

不满足以上任一条件的不视为被接纳。

### 3.2 社区 PR 清单

| # | PR 链接 | 标题 | 当前状态 | 判定依据 |
|---|---|---|---|---|
| 1 | `https://github.com/pytorch/pytorch/pull/xxxxx` | xxx | Merged / Approved | xxx |

### 3.3 torch_npu 同步变更

> 本任务是否需要 `torch_npu` 同步修改？

- [ ] **需要** → 填写下方 PR 清单。**要求：`torch_npu` PR 的描述中需附上对应的社区 PR 链接**，方便代码检视时对照社区侧变更。
- [ ] **不需要** → 说明原因：___

| # | PR 链接 | 标题 | 说明 |
|---|---|---|---|
| 1 | `https://gitcode.com/Ascend/pytorch/pull/xxxxx` | xxx | 删除 xx Patch，改为调用社区接口 |

---

## 4. 验证结果

### 4.1 运行结果汇总

| 硬件平台 | 涉及用例数 | 通过 | 失败 | 跳过 | 结论 |
|---|---|---|---|---|---|
| **A2/A3** | xx | xx | xx | xx | 通过 / 不通过 |
| **A5** | xx | xx | xx | xx | 通过 / 不通过 |

**验证环境：**

| 环境项 | 版本 |
|---|---|
| CANN 版本 | x.x.x |
| PyTorch 版本 | v2.x.x (commit: xxxxx) |
| torch_npu 版本 | vx.x.x (commit: xxxxx) |

**运行命令：**

```bash
python test/run_test.py -i xxx/xxx --hardware-requirement DEVICE_GENERIC
```

> 附：运行结果截图

### 4.2 失败分析

> 仅当存在失败用例时填写。若全部通过，本节写"无"即可。

| # | 失败用例 | 硬件 | 错误类型 | 根因分析 | 处理方式 | Issue 跟踪（必填） |
|---|---|---|---|---|---|---|
| 1 | `test_xxx_float16` | A5 | 精度偏差 | xxx 算子 float16 精度与社区参考不匹配 | 转 CANN 需求（JIRA: xxx） | `https://gitcode.com/Ascend/pytorch/issues/xxxxx` |
| 2 | `test_yyy` | A2/A3 | 功能不支持 | A2/A3 不支持 xxx 特性 | 记录为已知限制 | `https://gitcode.com/Ascend/pytorch/issues/xxxxx` |

> **错误类型可选值：** 精度偏差 / 功能不支持 / CANN 缺陷 / HCCL 缺陷 / 框架兼容性 / 用例标注错误 / 其他

> **处理方式可选值：** 记录为已知限制 / 转需求（附 JIRA 链接）/ 修改社区 PR / 修改 torch_npu / 待 SEG/SIG 评审

> **Issue 跟踪（必填）：** 每条失败用例均需创建 Issue 跟踪闭环，不允许遗漏。要求如下：
> - 相同根因的失败可合并到同一个 Issue
> - 处理方式为"记录为已知限制"的同样需要建 Issue 留档，供后续版本回溯
>
> **Issue 模板**（标题统一以 `【硬件解耦任务】` 开头，便于筛选）：
>
> - **① 背景：** 说明失败用例所属模块、解耦任务背景
> - **② 环境信息：** 包括 CANN、PyTorch、TorchNPU、OpPlugin、TritonAscend 的版本或 commitID
> - **③ 复现步骤：** 完整的运行命令及前置条件
> - **④ 报错日志：** 粘贴完整的错误堆栈或精度偏差日志
> - **⑤ 原因分析：** 对失败原因的初步分析

> **失败用例 Excel 维护：** 解耦前已获取的失败用例清单（Excel）需在解耦完成后由交付人员一并更新：逐条标注当前处置状态（已修复 / Issue 跟踪中），状态为"Issue 跟踪中"的需附上对应的 Issue 链接，确保每条失败用例有明确的结论和去向，不得留空。

### 4.3 SEG/SIG 评审

> 仅当存在失败且无法通过上述处理方式闭环时，提交 SE 汇总并上会评审。

| 评审项 | 内容 |
|---|---|
| 上会原因 | xxx |
| 评审日期 | YYYY-MM-DD |
| 评审结论 | 通过 / 有条件通过 / 退回 |
| 决议说明 | xxx |

---

## 5. 交付 Checklist

- [ ] **RFC 链接**（如涉及大范围接口变更，已有则贴链接，尚无则走 SE → SEG/SIG → 提交 RFC 流程）
- [ ] **社区 PR 链接**（已 Merged 或 Approved）
- [ ] **torch_npu PR 链接**（如需要同步修改，PR 描述中需附上社区 PR 链接）
- [ ] **torch_npu 无需修改原因**（如不需要同步修改）
- [ ] **A2/A3 运行结果截图**
- [ ] **A5 运行结果截图**
- [ ] **失败用例分析报告**（如有）
- [ ] **失败用例 Issue 跟踪链接**（如有失败需创建 Issue 跟踪，相同根因可合并，含"记录为已知限制"的项）
- [ ] **失败用例 Excel 更新**（解耦前清单逐条标注处置状态：已修复 / Issue 跟踪中，确保每条有结论）
- [ ] **SEG/SIG 评审结论**（如有失败且无法自行闭环）
